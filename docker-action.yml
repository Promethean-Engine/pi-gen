name: 'Build YggdrasilOS'
description: 'Runs the build script in a Docker container and makes the result available as a download.'

on:
  push:
    branches:
      - "master"

runs:
  using: 'docker'
  image: 'Dockerfile'
